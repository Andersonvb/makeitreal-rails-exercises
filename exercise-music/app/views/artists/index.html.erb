<h1 class='title' >Artists</h1>

<ul class='list-container' >
  <% @artists.each do |artist| %>
    <li class='card'>
      <img src="<%= artist.image_url %>" alt="Artist image">
      <h2><%= artist.name %></h2>
      <%= link_to "Ver más", artist_path(artist), class: 'btn' %>
    </li>
  <% end %>
</ul>
